<!-- Pegá esto dentro de <application> en AndroidManifest.xml -->
<receiver
    android:name=".KekeFitWidgetProvider"
    android:exported="false">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/keke_fit_widget_info" />
</receiver>
